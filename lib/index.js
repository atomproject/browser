let seedNew = require('./seed-new');
let generateSite = require('./generate-site');
let addElement = require('./add-element');

module.exports = {
  generateSite: generateSite,
  seedNew: seedNew,
  addElement: addElement
};
